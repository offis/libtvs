language: cpp

compiler:
  - clang
  - gcc

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libboost-dev libgtest-dev

install:
  - pip3 install meson

script: ./.run-ci-build.sh